id: itemio
name: ItemIO
version: "0.0.3"
description: Item input/output lib - Easy interaction between vanilla and custom containers
author: AirDox_

data_pack:
  load: [.]
#resource_pack:
#  load: [.]

extend: ["common.yaml"]

