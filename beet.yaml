id: itemio
name: ItemIO
version: "0.2.0"
description: Item input/output lib - Easy interaction between vanilla and custom containers
author: AirDox_

data_pack:
  load: [.]
#resource_pack:
#  load: [.]

extend: ["common.yaml"]

